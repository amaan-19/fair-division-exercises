// Algorithm ID
const algorithmId = "austins-moving-knife";

// Algorithm configuration
const algorithmConfig = {
    // Metadata
    name: "Austin's Moving Knife",
    description: "Continuous extension to Divide-and-Choose",
    playerCount: 2,
    type: "continuous",

    
};

window.FairDivisionCore.register(algorithmId, algorithmConfig);
